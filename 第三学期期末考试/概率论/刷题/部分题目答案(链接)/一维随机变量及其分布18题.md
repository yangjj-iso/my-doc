这是一个概率论中极为重要的结论。它解释了为什么我们可以通过 $Z = \frac{X-\mu}{\sigma}$ 将任意正态分布变换为标准正态分布，同时也说明了正态分布对**线性变换**具有“保型性”。

---

### 1. 均值与方差的推导

在证明分布类型之前，我们可以先利用期望和方差的线性性质直接求出 $Y$ 的数字特征：

- 均值 (Expectation)：
    
    $$E(Y) = E(aX + b) = aE(X) + b = a\mu + b$$
    
- 方差 (Variance)：
    
    $$D(Y) = D(aX + b) = a^2D(X) = a^2\sigma^2$$
    

因此，如果 $Y$ 服从正态分布，它的参数一定是 $Y \sim N(a\mu + b, a^2\sigma^2)$。

---

### 2. 分布性质的证明（分布函数法）

为了证明 $Y$ 仍然服从正态分布，我们需要推导出 $Y$ 的概率密度函数 $f_Y(y)$。

第一步：写出 $X$ 的概率密度函数

由于 $X \sim N(\mu, \sigma^2)$，其密度函数为：

$$f_X(x) = \frac{1}{\sqrt{2\pi}\sigma} \exp\left( -\frac{(x-\mu)^2}{2\sigma^2} \right)$$

第二步：利用分布函数法求 $Y$ 的分布函数 $F_Y(y)$

假设 $a > 0$（当 $a < 0$ 时证明过程类似）：

$$F_Y(y) = P\{Y \leq y\} = P\{aX + b \leq y\} = P\left\{ X \leq \frac{y - b}{a} \right\} = F_X\left( \frac{y - b}{a} \right)$$

第三步：求导获取 $Y$ 的密度函数 $f_Y(y)$

对 $F_Y(y)$ 关于 $y$ 求导：

$$f_Y(y) = F'_Y(y) = f_X\left( \frac{y - b}{a} \right) \cdot \frac{1}{a}$$

第四步：代入 $f_X$ 的表达式进行化简

$$f_Y(y) = \frac{1}{a} \cdot \frac{1}{\sqrt{2\pi}\sigma} \exp\left( -\frac{(\frac{y-b}{a} - \mu)^2}{2\sigma^2} \right)$$

将分母里的 $a$ 移入根号，并整理指数项：

$$f_Y(y) = \frac{1}{\sqrt{2\pi}(a\sigma)} \exp\left( -\frac{(y - (a\mu + b))^2}{2(a^2\sigma^2)} \right)$$

---

### 3. 结论

观察最后得到的 $f_Y(y)$ 表达式，它完全符合正态分布概率密度函数的结构：

- 新的均值为 $\mu_Y = a\mu + b$
    
- 新的方差为 $\sigma_Y^2 = a^2\sigma^2$
    

由此证得：**$Y = aX + b$ 也服从正态分布，即 $Y \sim N(a\mu + b, a^2\sigma^2)$。**

---

### 💡 核心点拨

- **线性变换不改变分布族**：正态分布经过线性变换还是正态分布。但要注意，如果是非线性变换（如 $Y = X^2$），结果通常就不再是正态分布（而是卡方分布）。
    
- **标准化的本质**：当你取 $a = \frac{1}{\sigma}, b = -\frac{\mu}{\sigma}$ 时，代入上面的结论：
    
    - 新均值：$\frac{1}{\sigma}\mu - \frac{\mu}{\sigma} = 0$
        
    - 新方差：$\left(\frac{1}{\sigma}\right)^2 \sigma^2 = 1$
        
        这完美解释了为什么标准化后的变量一定服从 $N(0, 1)$。
        

---

这个证明展示了正态分布在数学处理上的优越性。

**既然提到了 $X^2$，你想看看当 $X \sim N(0, 1)$ 时，$Y = X^2$ 的分布（即卡方分布的由来）是如何推导的吗？或者我们需要继续练习二维正态分布的性质？**